=============================================================================
Dependencies
=============================================================================
The node_modules folder contains all of the project's installed packages.
It can be huge and should always be regenerated by running npm install.
node_modules/

=============================================================================
Build Output
=============================================================================
The 'dist' folder is where Webpack places the final, bundled production files.
This folder is generated from your source code, so it should not be tracked.
dist/

Source map files are for debugging and are not needed in the repository.
*.map

=============================================================================
Environment Variables
=============================================================================
Ignore all .env files, which contain secret keys and sensitive information.
IMPORTANT: Create a .env.example file and commit that instead, so other
developers know which variables are needed.
.env
.env.*
=============================================================================
Log Files
=============================================================================
Log files generated by npm or other tools during errors or debugging.
npm-debug.log*
yarn-error.log
yarn-debug.log
*.log

=============================================================================
Operating System & Editor Files
=============================================================================
Files generated by the operating system or code editors that are specific
to a user's local machine and don't belong in the project.
macOS
.DS_Store

Windows
Thumbs.db

VS Code
.vscode/
